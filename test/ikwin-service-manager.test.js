/* global describe, it */

'use strict'

const assert = require('assert')
const sprintf = require('../lib/ikwin-service-manager.js').default

describe('ikwin-service-manager', function () {
    it('should just work', function () {
    })
})
